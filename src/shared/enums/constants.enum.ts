export enum Period {
  YEAR = 'year',
  MONTH = 'month',
  WEEK = 'week',
  TODAY = 'today',
  CUSTOM = 'custom',
}

export enum CompareDate {
  GREATER = 'GREATER',
  LESSER = 'LESSER',
  EQUAL = 'EQUAL',
}
